# Copilot instructions

This repository follows the **Cairn** convention. Read and follow `AGENTS.md` at the repository root. The full format is in `CAIRN.md`. Both are the single source of truth. This file is only a pointer.
