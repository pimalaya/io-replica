---
cairn: delta
change: guidelines-alignment
---

# Spec delta

## ADDED Requirements

None.

## MODIFIED Requirements

None. The spec describes the engine's merge and seam behaviour, which is untouched. The `client` feature removal, the error message prefixes and the logging placement are conventions the spec never pinned.

## REMOVED Requirements

None.
